# Session 2026-03-12

## Decisions
- First public alpha release will be `v0.0.1`.
- Release artifacts will be **unsigned / no-notarization** for now.
- Docker publishing will be skipped unless Docker credentials are configured.

## Work completed
- Updated `/projects/psyllama/urls.txt` to replace `psyllama.com` with `psyllama.com`.
  - `psyllama.ai` and `registry.psyllama.ai` were intentionally left unchanged.
- Updated GitHub Actions release workflow to support unsigned/no-notarization releases:
  - `.github/workflows/release.yaml`
    - macOS certificate import step is now conditional on `MACOS_SIGNING_KEY` being present.
    - Windows signing/auth steps are conditional on signing credentials being present; unsigned builds skip `sign` and `installer` steps.
    - Windows installer artifact upload is conditional on `dist/PsyllamaSetup.exe` existing.
    - Docker build/push jobs are skipped unless Docker credentials/vars are configured.

## Current status
- Release workflow should be able to build and draft a GitHub Release from a `v*` tag without any signing/notarization secrets.
- Local verification (tests + CLI smoke) is still pending.

## Next steps
- Run local validation (`go test ./...` + basic CLI smoke).
- Create and push tag `v0.0.1` to trigger the GitHub Actions release workflow.
